image: node:20-alpine

stages:
  - test
  - lint
  - security

cache:
  paths:
    - node_modules/

before_script:
  - npm ci --prefer-offline --ignore-scripts

test:
  stage: test
  script:
    - npm run test:ci

lint:
  stage: lint
  script:
    - npm run lint

security-audit:
  stage: security
  script:
    - npm audit
  allow_failure: true
